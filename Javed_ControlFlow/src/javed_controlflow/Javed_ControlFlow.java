/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package javed_controlflow;

/**
 *
 * @author ahlamjaved
 */
public class Javed_ControlFlow {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        //creates object
        MakingDecisions decide = new MakingDecisions();
        
        //do loop
        
        //calling the methods from outside the object- therefore need to give name of the object and the method name.
        /*do{
            decide.loopingType();
            decide.squaringNumber();
            decide.iterationNumber();
            decide.squareLoop();
        }while(decide.exitChoice());
        
        //instead of returning a variable, we are returning a boolean*/
        
        
    }
    
}
